# API origin only — no /api suffix (paths are built as ${VITE_API_URL}/api/...).
# Local:
#   VITE_API_URL=http://localhost:3000
# Production:
#   VITE_API_URL=https://api.example.com

VITE_API_URL=http://localhost:3000
